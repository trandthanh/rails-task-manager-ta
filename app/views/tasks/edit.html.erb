<%= form_for(@task) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :details %>
  <%= f.text_area :details %>

  <%= f.check_box :completed %> Completed

  <%= f.submit %>
<% end %>

<%= link_to "Back to list", tasks_path  %>
